# سهيل — Sprint 58

هذه الحزمة تجمع Sprints 48–54 فوق النسخة السابقة، وتركز على تثبيت الواجهة، فصل تحديد مستوى القدرات عن التحصيلي، تحسين الستريك، تعميق الأدمن، تأسيس حسابات آمنة وصداقات وتحديات عبر API، ثم الفحص والأداء والأمان.

## التصحيح المعتمد للمسارات

- الطالب يستطيع دراسة **القدرات والتحصيلي معًا**، أو اختيار أحدهما.
- **علمي/أدبي يخص القدرات فقط**:
  - قدرات علمي: 6 كمي + 6 لفظي في القياس الافتراضي.
  - قدرات أدبي: 4 كمي + 8 لفظي في القياس الافتراضي.
- **التحصيلي قياس مستقل وموحد**: رياضيات، فيزياء، كيمياء، وأحياء، ولا يتغير بسبب اختيار علمي أو أدبي في القدرات.
- لا يوجد اختبار تحديد مستوى مختلط. توجد نتيجتان وسجلان مستقلان: `qudrat` و`tahsili`.



## Sprint 56 — الوضع الداكن الشامل

- مفتاح الوضع الداكن في صفحة الحساب أصبح يغيّر مظهر **التطبيق كاملًا** فورًا.
- يشمل الرئيسية، المراجعة، المهام، الملخصات، تفاصيل الملخص، تحديد المستوى، إعداد الاختبار، الأسئلة، النتائج، الحساب، التحديات، الصفحات المحفوظة، وصفحات الأدمن.
- القائمة السفلية ولوحة الرفع داخل الملخص تتبعان الوضع الداكن.
- حفظ الاختيار في `localStorage` وتطبيقه مبكرًا قبل رسم الصفحات لتقليل وميض اللون الفاتح.
- تحديث لون واجهة النظام عبر `theme-color`، ودعم عناصر الإدخال والحالات الفارغة والبطاقات القديمة والجديدة.
- لا تُعكس الصور أو تتغير ألوان الرسومات؛ التغيير يقتصر على الخلفيات والأسطح والنصوص والحدود وعناصر التحكم.

## Sprint 55 — صفحة الحساب الجديدة

- إعادة تصميم صفحة الحساب وفق التقسيم المرجعي المعتمد: بطاقة موحدة كبيرة، صفوف واضحة، فواصل رفيعة، أيقونات خطية، وأسهم تنقل موحدة.
- الإبقاء على ملخص الحساب والشخصية والمسارات أعلى الصفحة دون ازدحام.
- إضافة وصول مباشر إلى رحلتي التعليمية، تحديد المستوى، الوضع الداكن، الإشعارات، الأصدقاء والتحديات، الأسئلة المحفوظة، عن سهيل، التواصل، الخصوصية، الشروط، والأسئلة الشائعة.
- الوضع الداكن يعمل مباشرة من نفس الصف.
- صفحات داخلية فعلية للإشعارات، التواصل، عن سهيل، الخصوصية، الشروط والأسئلة الشائعة.
- لوحة الأدمن وإدارة المحتوى تظهران في مجموعة مستقلة للأدمن فقط.
- عرض الإصدار وحقوق سهيل وزر تسجيل الخروج في نهاية الصفحة.

## التشغيل السريع على ويندوز

شغّل:

```text
run_suhail.bat
```

أو يدويًا:

```bash
pip install -r requirements.txt
streamlit run app.py
```

لتشغيل API المحلي:

```text
run_api.bat
```

ثم افتح `http://127.0.0.1:5000/health` للتأكد من عمله. نشر إعدادات الأدمن يحتاج ضبط متغير البيئة `SUHAIL_ADMIN_TOKEN` قبل تشغيل API.

## ما تغير في Sprints 48–54

### Sprint 48 — إعادة تأسيس الواجهة

- واجهة رئيسية نهائية واحدة تمنع عودة الصفحة القديمة.
- قائمة سفلية واحدة وثابتة مع Safe Area.
- القائمة ظاهرة في الصفحات العادية، وتتحول إلى لوحة مرفوعة داخل تفاصيل الملخص، وتختفي كليًا في الاختبار.
- القائمة مخفية في شاشة الدخول والافتتاحية.
- أيقونات SVG موحدة وإبراز متوازن للرئيسية.

### Sprint 49 — الرحلة والستريك

- نار SVG ظاهرة للستريك بدل الاعتماد على Emoji النظام.
- ساعة رملية SVG خلال آخر 6 ساعات قبل انقطاع السلسلة.
- تحديث عداد الوقت كل دقيقة.
- لا يثبت الستريك بمجرد فتح التطبيق؛ يحتاج نشاطًا تعليميًا.
- الصفحة الرئيسية تتمحور حول إجراء واحد: تحديد المستوى أو بدء جلسة اليوم.

### Sprint 50 — تدقيق بنك الأسئلة

- فحص 1000 سؤال تطويري.
- إصلاح 18 خطأ حسابيًا ناتجًا عن قطع الكسور في أسئلة النسب المئوية.
- وسم البنك كاملًا بأنه يحتاج مراجعة تحريرية وحقوقية.
- عدد الأسئلة المؤهلة للنشر حاليًا: **0** حتى الاعتماد البشري.

### Sprint 51 — تحديد المستوى ومؤشر الجاهزية

- تحديد مستوى مستقل للقدرات.
- تحديد مستوى مستقل للتحصيلي.
- علمي/أدبي يغير نموذج القدرات فقط.
- التحصيلي يستخدم `tahsili_common` لجميع الطلاب.
- حفظ نتائج القياسين في سجلين مستقلين محليًا وعبر API.
- تسمية النتيجة «مؤشر جاهزيتك في سهيل» مع تنبيه أنها ليست درجة رسمية.

### Sprint 52 — الأدمن والحسابات

- إعدادات أدمن بمسودة، استيراد/تصدير، نشر إلى API، سجل تغييرات، ومفاتيح للمحتوى والأداء والإشعارات.
- إزالة المستخدمين وكلمات المرور النصية من `data/users.json` في الوضع الإنتاجي.
- API حسابات يستخدم PBKDF2-HMAC-SHA256 وتوكنات جلسة.
- حماية إعدادات الأدمن بتوكن خادم.

### Sprint 53 — الأصدقاء والتحديات

- كود صداقة ثابت لكل حساب.
- إرسال طلب صداقة، عرضه، قبوله أو رفضه، ثم إنشاء علاقة للطرفين.
- إنشاء دعوة تحدٍ، قبولها، حفظ مجموعة الأسئلة، تسليم الإجابات، وحفظ نتيجة الطرفين.
- جداول SQLite وواجهات API مستقلة للملفات الشخصية والطلبات والأصدقاء والتحديات.

### Sprint 54 — الأداء والأمان والفحص

- API للأسئلة بصفحات وتصفية، كاش قصير، Gzip، ورؤوس حماية.
- فصل أصول التشغيل الخفيفة WebP عن المصادر الأصلية.
- فحص Python وJavaScript وSQLite وAPI وتدفقات الواجهة.
- تحقق بصري آلي من النار، القائمة، إخفاء التنقل في الاختبار، لوحة الملخص المرفوعة، ومنع عودة الرئيسية القديمة.

## API الأساسي

- `GET /health`
- `POST /api/v1/auth/register`
- `POST /api/v1/auth/login`
- `GET /api/v1/auth/me`
- `GET/PUT /api/v1/profile`
- `GET /api/v1/diagnostic?path=qudrat&track=literary` — يتطلب حسابًا ويعيد جلسة مؤقتة دون الإجابات الصحيحة.
- `GET /api/v1/diagnostic?path=tahsili` — يتطلب حسابًا ويتجاهل المسار الأكاديمي عمدًا.
- `POST /api/v1/diagnostic/<session_id>/submit` — تصحيح على الخادم وحفظ النتيجة.
- `GET /api/v1/diagnostic-results` — يعيد سجل القدرات والتحصيلي منفصلًا.
- `GET /api/v1/questions?test_format=محوسب&limit=50&offset=0` — يتطلب حسابًا، يدعم فصل المحوسب/الورقي، ويخفي الإجابات الصحيحة.
- `POST /api/v1/questions/grade` — تصحيح التدريب على الخادم.
- `POST/GET /api/v1/friend-requests`
- `POST /api/v1/friend-requests/<id>/respond`
- `GET /api/v1/friends`
- `POST/GET /api/v1/challenges`
- `GET /api/v1/challenges/<id>` — يعيد أسئلة التحدي دون الإجابات بعد تفعيله.
- `POST /api/v1/challenges/<id>/accept`
- `POST /api/v1/challenges/<id>/submit` — تصحيح ونتيجة مشتركة على الخادم.
- `GET/PUT /api/v1/admin/settings` مع `SUHAIL_ADMIN_TOKEN`

## الفحص

```bash
python scripts/validate_project.py
node --check src/ui/sprint54_experience.js
```

التقارير المهمة:

- `docs/reports/SPRINT_48_54_IMPLEMENTATION_REPORT.md`
- `docs/reports/SPRINT_54_ARCHITECTURE_AND_LIMITS.md`
- `docs/reports/SPRINT_54_CHECKS.json`
- `docs/reports/SPRINT_54_VISUAL_QA.json`
- `docs/reports/SPRINT_50_QUESTION_AUDIT.json`

## حدود يجب عدم تجاهلها قبل App Store

- واجهة Streamlit الحالية نموذج ويب متقدم، وليست تطبيق iPhone الأصلي النهائي.
- يلزم بناء React Native + Expo وربطه بالخادم ثم EAS Build/TestFlight.
- يلزم PostgreSQL/Redis وتخزين أصول عبر CDN بدل SQLite/JSON عند الإنتاج الكبير.
- إشعارات الجوال المغلق تحتاج Expo/APNs وخادم Push فعلي.
- بنك Sprint 58 مهيأ للاختبار الداخلي، لكن النشر العام يتطلب مراجعة بشرية نهائية للأسئلة اللفظية والصياغات والحقوق.


### Sprint 57 — الشخصيات بحسب نوع الحساب

- دمج 8 شخصيات رسمية: 4 أولاد و4 بنات.
- سؤال نوع الحساب موجود في التسجيل وإعداد الرحلة.
- الحساب الولد يرى شخصيات الأولاد فقط، والحساب البنت يرى شخصيات البنات فقط.
- حفظ النوع والشخصية في الملف المحلي وAPI، مع رفض عدم التطابق من الخادم.
- أربع قصّات محسّنة لكل شخصية: اختيار، أفاتار، نصف جسم، وكامل.
- أصول التشغيل بصيغة WebP وخلفية شفافة بدل تحميل الصور الأصلية الكبيرة.

تقرير التنفيذ: `docs/reports/SPRINT_57_CHARACTER_SYSTEM.md`.


## Sprint 58 — بنك القدرات المحوسب وتجربة السؤال

- استبدال بنك التطوير الظاهر داخل التطبيق ببنك محوسب مستقل من **280 سؤالًا**:
  - 195 سؤال قدرات كمي.
  - 85 سؤال قدرات لفظي.
- نقل بنك التطوير السابق ذي 1000 سؤال إلى `data/archive/questions_development_seed_s57.json` بدل خلطه بالبنك الفعلي.
- جميع المسائل الرقمية أُنشئت بأرقام جديدة، وأعيد حساب الإجابة الصحيحة والخيارات بعد التعديل.
- الأسئلة اللفظية وقطع الاستيعاب صيغت من جديد؛ استُخدمت التجميعات لتحديد الأنماط فقط.
- توزيع الإجابة الصحيحة متوازن تمامًا: 70 سؤالًا لكل موضع من أ/ب/ج/د.
- إضافة 21 رسمًا أصليًا خفيفًا للهندسة والبيانات.
- كل سؤال يحمل بيانات ربط مخفية: `keywords`, `concept_id`, `misconception_id`, و`summary_block_id` تمهيدًا للربط الدقيق بالملخصات.
- وسم البنك كاملًا بأنه **محوسب**؛ الورقي منفصل ولا يُفعّل حتى إضافة تجميعاته.
- إعادة تنسيق السؤال والقطعة والرسوم والخيارات وشريط التقدم لتكون متناسقة على شاشة الجوال والوضع الداكن.
- إزالة أسماء الشخصيات من بطاقات الاختيار، وتحسين المساحة العلوية للشخصية الأنثوية الأولى.

التقارير:

- `docs/reports/SPRINT_58_QUESTION_BANK_REPORT.json`
- `docs/reports/SPRINT_58_CHECKS.json`
- `docs/reports/SPRINT_58_IMPLEMENTATION.md`


## فحص Sprint 58

- تحقق بنيوي من 280 سؤالًا والإجابات والخيارات والرسومات وقاعدة SQLite.
- تحقق مرئي على مقاس iPhone لإعداد الاختبار، سؤال كمي مصور، قطعة استيعاب، والوضع الداكن.
- تشغيل Streamlit فعليًا واجتياز نقطة الصحة `HTTP 200 / ok`.

التقارير:

- `docs/reports/SPRINT_58_IMPLEMENTATION.md`
- `docs/reports/SPRINT_58_QUESTION_BANK_REPORT.json`
- `docs/reports/SPRINT_58_CHECKS.json`
- `docs/reports/SPRINT_58_VISUAL_QA.json`
