# Sprint 64 — إصلاح فتح صفحة الاختبار

## سبب الخلل
زر «اختبار» في Sprint 63 كان يستدعي `goToExercise()`، وهذه الدالة تعيد المستخدم إلى `homePage` بدل فتح إعداد الاختبار؛ لذلك بدا الزر وكأنه لا يعمل.

## الإصلاح
- أصبح الزر يفتح نافذة واضحة لاختيار:
  - قدرات كمي
  - قدرات لفظي
- كل اختيار يستدعي `openExamSetup(exam)` مباشرة.
- أضيف مسار احتياطي لفتح `exercisePage` عند غياب الدالة الأساسية.
- النافذة تدعم الوضع الداكن والإغلاق بالضغط خارجها.

## التحقق
- Python compile: passed.
- JavaScript syntax: passed.
- لم يُجرَ تشغيل Streamlit داخل بيئة التسليم لعدم توفر أمر `streamlit` في الحاوية الحالية.
